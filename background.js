// background.js
// hanya untuk debugging / permintaan di masa depan.
// saat ini tidak banyak logic di service worker
chrome.runtime.onInstalled.addListener(() => {
  console.log("AutoFill extension installed");
}); 